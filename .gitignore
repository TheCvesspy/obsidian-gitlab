# Build outputs
main.js
main.js.map
*.js.map
build/
dist/

# styles.css is generated by esbuild from src/styles/*.css.
# It is intentionally still tracked because it ships in the plugin bundle.
# To stop seeing local diffs after running `npm run build`, run:
#   git update-index --skip-worktree styles.css
# (don't `git rm --cached` — the file must remain in the published plugin)

# Dependencies
node_modules/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local config
.claude/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local

# Test coverage
coverage/

# Temporary files
*.tmp
.cache/
